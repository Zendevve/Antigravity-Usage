# Development Setup

## Prerequisites
- Node.js (v16+)
- VS Code

## Setup
1. `npm install`
2. `npm run compile` (Watch mode: `npm run watch`)
3. Press F5 to launch the Extension Development Host.

## Project Structure
- `src/`: Source code
- `test/`: Tests
- `package.json`: Manifest
